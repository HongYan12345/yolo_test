冒险岛自动战斗辅助 - 图像识别模块需求说明（修订版）

> 修订结论：方案总体可行。当前核心数据只有两类：`monster`（怪物）和 `item`（掉落物）。考虑到真实游戏截图无法穷尽所有怪物、掉落物、地图和状态，数据策略调整为“两个网站的怪物/物品素材作为 monster 与 item 外观覆盖的主要来源，合成数据作为主要扩充手段，少量真实游戏截图用于校准、微调和验收”。注意：网站透明 PNG 或物品图标不能原样直接替代实战截图，必须经过背景合成、缩放扰动、遮挡扰动、特效/噪声模拟和真实截图回归验证。本需求仅描述图像识别模块及其与控制脚本的数据接口，不包含规避游戏安全机制、反检测、账号风控绕过等内容。

## 1. 项目目标

开发一个基于图像识别的实时检测模块，识别游戏画面中的怪物和掉落物，并将位置信息传递给按键控制脚本，用于实现“在当前地图自动打怪、自动拾取掉落物”的基础闭环。

当前范围：

- 检测怪物。
- 检测可拾取掉落物。
- 输出目标类别、置信度、边界框坐标。
- 不涉及地图类型识别、地形分析、路径规划、反检测、绕过安全机制等高级功能。

## 2. 可行性评估

### 2.1 可行部分

使用 Ultralytics YOLOv8/YOLO11 训练一个两类目标检测模型，在固定分辨率、固定游戏窗口、固定截图区域下识别怪物和掉落物，是可行的。

适合先做 MVP：

- 地图数量控制在 3~5 张。
- 怪物种类控制在 MVP 地图内实际出现的怪。
- 掉落物先统一识别为 item，不细分金币、装备、消耗品。
- 推理区域固定，例如 800×600 或游戏窗口内有效战斗区域。
- 控制脚本只消费检测结果，不把移动路径规划复杂化。

### 2.2 原方案中的主要不可行点

1. **网站素材不能“原样”直接作为最终训练图片**

   由于真实截图无法穷尽，mxdzlk/osmlib 的怪物素材和物品素材应作为 `monster` 与 `item` 外观覆盖的主要来源。但网站爬到的怪物 PNG、物品图标或素材图通常是透明背景、标准姿态、干净展示图；实际游戏截图中存在地图背景、血条/伤害数字、技能特效、遮挡、缩放、压缩、半透明、运动帧等情况。因此不能把网站素材原样直接丢给模型训练后上线，必须转换为贴近真实游戏画面的合成样本，并使用少量真实截图做校准和验收。

2. **掉落物 item 是与怪物同等重要的核心数据**

   本项目不是只做怪物检测，`monster` 和 `item` 是两个核心类别。掉落物数据需要单独建立素材库和样本集。可以优先从 mxdzlk/osmlib 或其他资料库收集金币、装备、消耗品等物品素材/图标，但必须通过真实截图验证其地面掉落形态是否与网站图标一致；如果不一致，需要额外采集地面掉落物截图或专用掉落物素材。

3. **真实截图不需要穷尽，但必须覆盖域差异**

   真实截图无法覆盖全部怪物、掉落物和地图，因此不应把真实截图数量作为唯一数据来源。建议由网站怪物/物品素材负责“monster/item 外观覆盖”，由合成数据负责“场景组合覆盖”，由真实截图负责“域差异校准、阈值选择、误检/漏检修正和最终验收”。

4. **直接缩放到 640×640 不严谨**

   游戏截图通常是 4:3 或 16:9，直接拉伸到正方形会改变怪物比例。训练和推理都应使用 YOLO 的 letterbox 等比例缩放，并在后处理时正确还原坐标。

5. **普通 CPU 单帧 ≤50ms 偏乐观**

   yolov8n/YOLO11n 在不同 CPU 上差异较大。若要求稳定 10 FPS，建议提供 640、512、416、320 多档输入尺寸，并支持跳帧检测、ROI 检测或 GPU/DirectML/CUDA 推理。

6. **默认数据增强需要调整**

   水平翻转通常可用；垂直翻转不符合游戏画面逻辑，应禁用。Mosaic 对小精灵目标有帮助，但后期建议关闭或减弱，避免生成不真实的空间关系。

## 3. 功能需求

### 3.1 怪物检测

- 实时检测游戏画面中出现的所有可攻击怪物。
- 输出每个怪物的边界框坐标。
- 同一帧内支持多个怪物。
- 对严重遮挡、极小目标可允许漏检，但需要在标注规范中统一处理。

输出字段建议：

```json
{
  "class_id": 0,
  "class_name": "monster",
  "confidence": 0.87,
  "bbox": [x1, y1, x2, y2],
  "center": [cx, cy]
}
```

### 3.2 掉落物检测

- 实时检测画面中的可拾取掉落物。
- MVP 阶段统一为 item 类，不细分具体物品类型。
- 输出每个掉落物的边界框坐标。
- 掉落物尺寸通常较小，应在采集和训练时保证足够的小目标样本。

### 3.3 多类别统一检测

采用单一目标检测模型同时输出怪物与掉落物两类目标。

类别定义：

```text
0: monster
1: item
```

### 3.4 目标优先级逻辑

优先级逻辑由脚本侧实现，模型只负责识别。

建议策略：

1. 如果检测到掉落物，且 item 置信度大于阈值，并且距离角色较近，则优先拾取。
2. 否则选择最近怪物或置信度最高怪物作为攻击目标。
3. 如果无目标，则进入脚本侧巡逻/等待逻辑。

模型不负责：

- 判断角色是否能到达目标。
- 判断地形平台。
- 判断技能释放时机。
- 判断地图路径。

## 4. 数据集方案

### 4.1 数据来源优先级

数据来源按优先级排序：

1. **mxdzlk/osmlib 怪物素材：作为 monster 外观覆盖的主要来源。**
2. **mxdzlk/osmlib 物品/掉落物素材：作为 item 外观覆盖的重要来源。**
3. **合成数据：作为训练集扩充的主要手段，把网站怪物素材和掉落物素材贴到真实或半真实地图背景中。**
4. **真实游戏截图标注数据：不要求穷尽，但必须用于校准、微调、验证和测试。**
5. **真实截图主动学习补标：用于持续修正实战漏检、误检和小目标问题。**

核心原则：

- monster 类主要依赖两个网站的怪物素材覆盖种类。
- item 类也要作为独立核心数据集建设，优先收集金币、装备、消耗品、其他可拾取物的素材。
- 如果网站物品图标与地面掉落形态一致或接近，可用于合成 item 训练图。
- 如果网站物品图标与地面掉落形态差异明显，必须补充真实地面掉落物截图或专用掉落物素材。
- 验证集和测试集必须保留真实截图，不能只用合成图自测。

### 4.2 真实截图采集

使用 mss 等截图工具按固定区域抓取游戏画面，截图区域坐标写入配置文件。

采集要求：

- 固定游戏分辨率、窗口缩放、UI 缩放。
- 覆盖至少 3~5 张目标刷怪地图。
- 每张地图覆盖不同站位、不同怪物密度、不同技能特效。
- 包含怪物/掉落物处于画面边缘、重叠、半遮挡、特效干扰、伤害数字遮挡等场景。
- 包含负样本，即没有怪物或没有掉落物的截图，降低误检。

MVP 建议真实截图样本量（用于校准和验收，不要求穷尽）：

- 真实原始截图总量：建议 500~1000 张起步。
- 含怪物截图：不少于 300 张。
- 含掉落物截图：不少于 300 张。
- 纯背景/无目标负样本：不少于 100 张。
- 每张目标地图至少保留 80~150 张真实截图，用于检查背景误检和实战域差异。

网站/合成数据建议规模：

- 从 mxdzlk/osmlib 获取尽可能完整的目标怪物素材。
- 从 mxdzlk/osmlib 或其他资料库获取尽可能完整的掉落物/物品素材。
- 第一版合成训练图建议 3000~10000 张。
- 每个目标怪物至少合成 20~100 个不同背景、缩放、遮挡和亮度版本。
- 每类常见掉落物至少合成 50~200 个不同背景、缩放、遮挡和亮度版本。
- 对 MVP 地图中的高频怪物和高频掉落物应增加合成数量和真实截图校准样本。

### 4.3 爬虫素材的正确用途

爬虫得到的怪物透明 PNG 和物品/掉落物素材应作为两个核心类别的主要外观来源，用于：

- 快速获得大量 monster 外观先验。
- 快速获得大量 item 外观先验。
- 覆盖真实截图难以穷尽的怪物种类和掉落物种类。
- 与真实地图背景或采集到的空场景背景合成训练样本。
- 增强不同缩放、亮度、轻微遮挡、边缘裁切、伤害数字遮挡等情况。
- 辅助冷启动，减少真实截图标注压力。

爬虫素材不能用于：

- 原样直接替代真实游戏画面。
- 直接评估模型实战效果。
- 在没有真实截图校准的情况下直接上线。

合成数据建议控制比例：

- 第一版训练中，网站素材生成的合成图可占训练集 60%~80%。
- 真实截图可占训练集 20%~40%，主要用于校准域差异。
- 验证集和测试集必须以真实截图为主，建议 100% 使用真实截图。
- 最终模型建议至少经过一轮真实截图微调或主动学习补标。

### 4.4 掉落物 item 数据采集

掉落物是本项目与怪物并列的核心数据，不能作为附属样本处理。

item 数据来源建议：

1. **网站物品/掉落物素材**
   - 从 mxdzlk/osmlib 或其他资料库获取金币、装备、消耗品、其他可拾取物素材。
   - 如果素材是透明 PNG 或图标，可用于合成地面掉落样本。
   - 需要确认网站图标与游戏内地面掉落形态是否一致或足够接近。

2. **真实地面掉落物截图**
   - 用于校准网站素材和真实游戏画面的差异。
   - 用于补充金币、装备、消耗品在不同地图背景上的小目标表现。
   - 用于验证 item 检测是否能在真实背景、技能特效和遮挡下工作。

3. **合成 item 数据**
   - 将物品素材贴到地图地面、平台或角色附近。
   - 加入阴影、缩放、轻微透明度、亮度扰动和边缘遮挡。
   - 自动生成 YOLO 标签。

标注策略：

- MVP 阶段统一标注为 `item`，不细分金币、装备、消耗品。
- 如果后续拾取策略需要区分类型，可扩展为 `coin/equip/consume/other`，但第一版不建议拆分类别。
- item 是小目标，验证集必须包含足够多的真实地面掉落物截图。

### 4.5 标注规范

使用 LabelImg、CVAT、Roboflow 或 Label Studio，以 YOLO 格式标注。

标注规则：

- 每个可见怪物标注一个紧贴主体的矩形框。
- 每个可拾取掉落物标注一个紧贴主体的矩形框。
- 严重遮挡且无法判断完整主体的目标可不标。
- 小于 12×12 像素的 item 可根据实际拾取需求决定是否标注；如果希望能拾取小金币，则必须保留并增加高分辨率训练。
- 小于 16×16 像素的 monster 可忽略。
- 角色自身、宠物、技能特效、NPC、背景装饰不标注。
- 同一批数据必须保持一致，不能一部分标注宠物、一部分不标注。

### 4.6 数据集划分

不能简单随机打散全部截图，建议按地图和时间片混合划分，避免连续帧泄漏。

推荐：

- train：70%
- val：20%
- test：10%

如果数据较少：

- train：80%
- val：20%
- 另保留一小组未参与训练的实战截图作为人工回归测试集。

目录结构：

```text
datasets/
  images/
    train/
    val/
    test/
  labels/
    train/
    val/
    test/
  data.yaml
```

`data.yaml` 示例：

```yaml
path: ./datasets
train: images/train
val: images/val
test: images/test
nc: 2
names:
  0: monster
  1: item
```

## 5. 模型训练技术要求

### 5.1 架构选择

推荐起步模型：

- YOLOv8n 或 YOLO11n：优先速度，适合实时推理。
- YOLOv8s 或 YOLO11s：精度更高，但 CPU 推理压力更大。

MVP 建议：

- 先用 nano 模型跑通完整数据闭环。
- 如果漏检严重，再尝试 small 模型或提高输入分辨率。

### 5.2 输入尺寸

建议训练尺寸按性能分档：

- 高精度：640
- 平衡：512
- 高速度：416 或 320

注意事项：

- 训练和推理必须使用一致的 letterbox 预处理。
- 不建议直接把 800×600 拉伸成 640×640。
- 对 item 小目标检测不佳时，优先尝试更大输入尺寸或裁剪 ROI，而不是盲目增加模型大小。

### 5.3 训练配置

基础配置：

```bash
yolo detect train model=yolov8n.pt data=datasets/data.yaml imgsz=640 epochs=100 patience=20 batch=8 device=0
```

无 GPU 时：

```bash
yolo detect train model=yolov8n.pt data=datasets/data.yaml imgsz=512 epochs=80 patience=15 batch=4 device=cpu
```

增强建议：

- `fliplr` 可保留。
- `flipud=0`，禁用上下翻转。
- 合理使用 hsv 色彩扰动。
- 后 10~20 个 epoch 减弱或关闭 mosaic。
- 不要使用会明显破坏游戏画面结构的强透视变换。

### 5.3.1 GTX 1080 训练可行性与推荐配置

使用 GTX 1080 训练本项目 MVP 模型是可行的。

硬件判断：

- GTX 1080 通常为 8GB 显存，足够训练两类目标检测模型的 nano/small 版本。
- 本项目当前只有 `monster` 和 `item` 两类，类别数量少，不会成为显存瓶颈。
- 主要显存压力来自输入尺寸、batch size、模型大小和数据增强，而不是类别数。
- GTX 1080 属于 Pascal 架构，没有 Tensor Core，FP16/AMP 不一定明显提速；如果训练不稳定或速度收益不明显，可以关闭 AMP。
- 对小目标 item 来说，优先保留较高输入尺寸，例如 640；如果显存不足，先降低 batch，而不是一开始就把 imgsz 降到 320。

推荐训练环境：

- Python 建议优先使用 3.10 或 3.11；如果本机已有 Python 3.12，也可以使用 3.12，但需要使用较新的 PyTorch、Ultralytics、ONNX Runtime 版本，并确认 CUDA 版 PyTorch 能正常安装。
- 爬虫脚本只依赖 Python 标准库，在 Python 3.10/3.11/3.12 下都可以直接运行；为减少环境切换成本，建议爬虫、数据合成、训练、推理尽量使用同一个 Python 虚拟环境。
- 不建议用 Python 3.14 作为本项目主环境，因为 PyTorch、Ultralytics、ONNX Runtime GPU 等训练/推理依赖可能尚未完整支持。
- 安装支持 CUDA 的 PyTorch 版本，确认 `torch.cuda.is_available()` 为 `True` 后再开始正式训练。
- NVIDIA 驱动需要保持较新版本；通常不需要单独安装完整 CUDA Toolkit，优先使用 PyTorch 官方 CUDA wheel。

GTX 1080 推荐从以下配置起步：

```bash
yolo detect train model=yolov8n.pt data=datasets/data.yaml imgsz=640 epochs=100 patience=20 batch=8 device=0 workers=4 amp=False
```

如果出现显存不足，按顺序调整：

```bash
yolo detect train model=yolov8n.pt data=datasets/data.yaml imgsz=640 epochs=100 patience=20 batch=4 device=0 workers=4 amp=False
```

如果仍然显存不足，再降输入尺寸：

```bash
yolo detect train model=yolov8n.pt data=datasets/data.yaml imgsz=512 epochs=100 patience=20 batch=8 device=0 workers=4 amp=False
```

如果 nano 模型漏检明显，可以尝试 small 模型，但 batch 需要降低：

```bash
yolo detect train model=yolov8s.pt data=datasets/data.yaml imgsz=640 epochs=100 patience=20 batch=4 device=0 workers=4 amp=False
```

建议训练路线：

1. 先用 `yolov8n.pt + imgsz=640 + batch=4/8` 跑通数据闭环。
2. 如果 item 小目标漏检严重，优先增加真实 item 样本、提高合成质量、保留 640 输入尺寸。
3. 如果 monster 精度够但 item 仍差，可尝试 ROI 二次检测或增加 item 样本，而不是直接上大模型。
4. 如果整体精度不足，再尝试 `yolov8s.pt`。
5. GTX 1080 不建议第一版直接训练 medium/large 模型，性价比不高且显存压力大。

### 5.4 主动学习迭代流程

推荐训练流程：

1. 从 mxdzlk/osmlib 获取目标怪物素材和 ID 清单。
2. 从 mxdzlk/osmlib 或其他资料库获取目标掉落物/物品素材和 ID 清单。
3. 采集真实空地图/少怪地图背景，以及少量真实战斗截图和真实掉落物截图。
4. 使用网站怪物素材和掉落物素材生成大规模合成数据。
5. 标注第一批真实截图，重点覆盖 monster、item、技能特效、遮挡和背景误检。
6. 使用“合成数据 + 真实截图”训练 v0 模型。
7. 用 v0 在未标注真实截图或实战录像帧上推理。
8. 收集 monster/item 的漏检、误检、高不确定样本。
9. 人工修正并加入训练集。
10. 训练 v1/v2。
11. 使用固定真实 test 集和实战录屏回归测试。

### 5.5 评估指标

主要参考：

- mAP50
- mAP50-95
- Precision
- Recall
- 漏检率
- 误检率
- 实战帧率

MVP 验收指标建议：

- monster 类 mAP50 ≥ 0.85
- item 类 mAP50 ≥ 0.70
- 整体 mAP50 ≥ 0.80
- 实战录屏中怪物明显漏检率 ≤ 10%
- 实战录屏中 item 明显漏检率 ≤ 20%

说明：

- item 小目标比 monster 更难，第一版 item mAP50 ≥ 0.75 不一定稳定，应允许通过迭代提升。
- 验证集和测试集必须来自真实截图。

## 6. 模型导出与部署

### 6.1 导出格式

训练完成后导出 ONNX：

```bash
yolo export model=runs/detect/train/weights/best.pt format=onnx opset=12 imgsz=640 simplify=True
```

要求：

- 保留 `best.pt` 作为备用。
- 导出 `best.onnx` 用于部署。
- 训练和部署输入尺寸一致。
- batch 固定为 1，便于实时推理。

### 6.2 推理依赖

Python 版本：

- Python 3.10、3.11 或 3.12。优先推荐 3.10/3.11；如果本机已有 3.12，可以直接尝试 3.12，但需要安装较新的 ONNX Runtime、OpenCV 等依赖版本。

依赖：

```text
opencv-python
mss
numpy
onnxruntime
pydirectinput
pywin32（可选，用于窗口检测）
```

如有 NVIDIA GPU：

```text
onnxruntime-gpu
```

### 6.3 推理流程

1. 按配置坐标截取游戏画面。
2. 对截图执行 letterbox 等比例缩放。
3. BGR/RGBA 转 RGB。
4. 归一化到 0~1。
5. HWC 转 CHW。
6. 添加 batch 维度。
7. ONNX Runtime 推理。
8. 解析输出。
9. 执行置信度过滤和 NMS。
10. 将边界框还原到原始截图坐标。
11. 输出检测结果列表给控制模块。

输出结果示例：

```json
[
  {
    "class_id": 0,
    "class_name": "monster",
    "confidence": 0.91,
    "bbox": [325, 214, 374, 268],
    "center": [349, 241]
  },
  {
    "class_id": 1,
    "class_name": "item",
    "confidence": 0.78,
    "bbox": [514, 421, 536, 442],
    "center": [525, 431]
  }
]
```

## 7. 性能要求

### 7.1 MVP 性能目标

在普通 CPU 上：

- yolov8n/YOLO11n，imgsz=416 或 512，目标整体循环 ≥ 8 FPS。
- 如果 imgsz=640 无法达到实时要求，可降低输入尺寸或只检测战斗 ROI。

在 GPU 上：

- imgsz=640，目标整体循环 ≥ 15 FPS。

### 7.2 优化策略

- 降低输入尺寸：640 → 512 → 416。
- 只截取游戏战斗区域，排除 UI 区域。
- 每 N 帧检测一次，中间帧复用上一帧结果。
- 使用 ONNX Runtime GPU、TensorRT 或 OpenVINO。
- 将截图、推理、控制拆成异步队列。
- 对 item 可在角色附近 ROI 中二次检测，提高小目标召回。

## 8. 脚本集成要求

### 8.1 模块化

图像识别模块封装为独立类或函数：

```python
detector = MapleDetector("best.onnx", config)
detections = detector.detect(frame)
```

控制模块只接收 detections，不关心模型内部实现。

### 8.2 配置项

建议集中在 `config.yaml` 或 `config.json`：

```yaml
capture:
  left: 0
  top: 0
  width: 800
  height: 600

model:
  path: "models/best.onnx"
  input_size: 512
  conf_threshold:
    monster: 0.45
    item: 0.55
  iou_threshold: 0.45

runtime:
  provider: "CPUExecutionProvider"
  target_fps: 10

control:
  pickup_distance: 180
  attack_distance: 260
```

### 8.3 稳定性

必须处理：

- 游戏窗口不存在。
- 游戏窗口最小化。
- 截图失败。
- ONNX 推理异常。
- 检测结果为空。
- 坐标越界。
- 按键控制异常。

建议提供紧急停止热键，例如 F12 或 Ctrl+Esc。

## 9. 交付物

必须交付：

1. 数据集
   - 真实截图训练集/验证集/测试集。
   - YOLO 标签文件。
   - `data.yaml`。
   - 数据集说明：采集地图、分辨率、截图区域、类别定义。

2. 爬虫/素材工具
   - 怪物素材下载脚本。
   - 下载清单。
   - 素材来源记录。
   - 合成数据生成脚本（如使用）。

3. 模型文件
   - `best.pt`
   - `best.onnx`
   - 训练日志和评估结果。

4. 识别模块代码
   - 截图。
   - letterbox 预处理。
   - ONNX 推理。
   - NMS 后处理。
   - 坐标还原。
   - 检测结果输出。

5. 演示脚本
   - 接入识别模块。
   - 能打印或可视化检测结果。
   - 可选接入按键控制。

6. 使用说明
   - 环境安装。
   - 截图区域配置。
   - 模型路径配置。
   - 置信度阈值配置。
   - 运行步骤。
   - 常见问题排查。

## 10. 推荐落地路线

第一阶段：网站素材与合成数据闭环

- 从 mxdzlk/osmlib 获取目标怪物素材。
- 从 mxdzlk/osmlib 或其他资料库获取目标掉落物/物品素材。
- 固定游戏窗口和截图区域。
- 采集空地图背景、少量真实战斗截图和真实掉落物截图。
- 生成 3000~10000 张 monster/item 合成训练图。
- 标注 500~1000 张真实截图用于校准、验证和测试。
- 训练 yolov8n。
- 导出 ONNX。
- 做实时可视化检测，不接控制。

第二阶段：实战修正

- 用模型跑真实录屏。
- 收集漏检、误检帧。
- 补标并重新训练。
- 确认 3~5 张地图表现稳定。

第三阶段：接入控制

- 仅在检测稳定后接入控制脚本。
- 先做日志模式，再做半自动模式。
- 加入紧急停止和窗口状态检测。

第四阶段：优化性能

- 根据硬件选择 imgsz。
- 尝试 GPU/OpenVINO/TensorRT。
- 对小目标 item 做 ROI 或高分辨率策略。

## 11. 最终判断

修订后的方案可行。使用 GTX 1080 训练本项目 MVP 识图模型也可行，推荐以 `YOLOv8n/YOLO11n + imgsz=640 + batch=4~8` 起步；如显存不足优先降低 batch，如精度不足再尝试 small 模型。环境建议优先使用 Python 3.10 或 3.11；如果当前电脑已经安装 Python 3.12，也可以直接用 3.12 建虚拟环境运行爬虫、数据处理、训练和推理，但需要确认 PyTorch、Ultralytics、ONNX Runtime 等依赖能正常安装，且 `torch.cuda.is_available()` 为 `True`。当前项目不建议继续使用 Python 3.14 虚拟环境作为主环境。

关键前提：

- mxdzlk/osmlib 数据可以作为 monster 外观覆盖的主要来源。
- mxdzlk/osmlib 或其他资料库的物品/掉落物素材可以作为 item 外观覆盖的重要来源。
- monster 和 item 是两个同等重要的核心数据集，都需要素材库、合成数据和真实截图校准。
- 爬虫图片不能原样直接训练上线，必须通过合成、域随机化和真实截图校准来贴近实战画面。
- 验证集和测试集必须以真实游戏截图为主。
- 推理预处理必须采用 letterbox 并正确还原坐标。
- 性能目标需要按硬件和输入尺寸分档，不应固定承诺普通 CPU 640 输入 ≤50ms。
