冒险岛怪物与掉落物素材爬取与训练数据使用说明（修订版）

> 结论：当前核心数据只有两类：`monster`（怪物）和 `item`（掉落物）。由于真实游戏截图无法穷尽全部怪物、掉落物和场景，mxdzlk/osmlib 两个网站的数据应作为 monster 与 item 外观覆盖的重要来源。怪物透明 PNG、物品图标或掉落物素材适合做素材库、合成数据和冷启动训练；但不能原样直接替代真实游戏截图，必须通过背景合成、域随机化、遮挡/特效模拟，并使用少量真实截图做校准、微调和验收。

## 1. 爬虫数据在本项目中的定位

本项目目标是训练一个能在真实游戏画面中检测 `monster` 和 `item` 的两类目标检测模型。

由于真实截图无法穷尽，爬虫数据的正确定位应调整为：

- 作为 `monster` 类外观覆盖的主要来源。
- 作为 `item` 类外观覆盖的重要来源。
- 用于尽可能覆盖不同怪物 ID、造型、Boss、活动怪、任务怪。
- 用于尽可能覆盖金币、装备、消耗品、其他可拾取物等掉落物外观。
- 用于制作大规模 monster/item 合成训练图。
- 用于冷启动训练，降低真实截图标注压力。
- 与少量真实截图结合，形成“网站素材覆盖 + 合成场景泛化 + 真实截图校准”的训练闭环。

爬虫数据不能：

- 原样直接作为最终训练图片。
- 单独代表真实游戏截图分布。
- 替代 item 的真实地面掉落形态校准。
- 替代真实截图验证集和测试集。

原因：

- 网站素材多为透明背景或标准展示图。
- 实战画面有地图背景、技能特效、伤害数字、遮挡、运动帧、缩放和压缩。
- 掉落物在地面上的表现可能不同于网站物品图标，因此 item 也需要真实截图校准。
- 只用透明 PNG 原图训练会产生明显域差异，实战漏检和误检概率较高。
- 正确做法是把透明 PNG 转换成贴近真实游戏画面的合成样本，再用少量真实截图校准。

## 2. 网站一：mxdzlk.com（冒险岛资料库）

### 2.1 页面形式

怪物详情页：

```text
https://mxdzlk.com/monster/{id}
```

示例：

```text
绿蜗牛：https://mxdzlk.com/monster/100100
扎昆：https://mxdzlk.com/monster/9300000
```

### 2.2 ID 规律

怪物 ID 不是连续完整整数。

常见规律：

- 常规怪物多为 6 位，例如 `100100`。
- Boss、活动、副本怪物可能为 7 位，例如 `9300000`。
- `100xxx ~ 900xxx` 常见于普通怪物。
- `90xxxxx ~ 93xxxxx` 常见于 Boss 或特殊怪物。
- 其他高位段可能包含活动、副本、任务专属怪物。

注意：

- 不能直接从 `100000` 暴力遍历到 `9999999`。
- 中间存在大量空洞。
- 应优先从列表页、站点数据或公开 ID 数据集获取有效 ID。

### 2.3 图片获取方式

当前已验证的 mxdzlk 怪物列表页会直接暴露静态图片地址，实际可用格式为：

```text
https://static.mapleartale.com/images/mob/{id}.png
```

因此当前项目已经采用更高效的列表页解析方式：

1. 请求怪物列表页：`https://mxdzlk.com/monster/?page_num=N`。
2. 从列表页 HTML 中提取 `static.mapleartale.com/images/mob/{id}.png`。
3. 同时解析怪物详情页链接、ID、名称等元数据。
4. 下载图片到 `assets/monsters/raw/{id}.png`。
5. 写入 `assets/monsters/metadata.csv`。

说明：

- 当前不再使用 `wp-content/uploads/monsters/{id}.png` 作为主路径。
- 不建议暴力拼接详情页或旧图片路径。
- 如果后续网站结构变化，应重新探测 HTML 或接口，再更新爬虫解析规则。

## 3. 网站二：osmlib.com（开源冒险岛库）

### 3.1 页面形式

主站：

```text
https://osmlib.com/?locale=zh-CN
```

怪物详情页可能为：

```text
https://osmlib.com/monsters/{id}
```

### 3.2 ID 规律

osmlib 更接近游戏内部 ID，ID 长度不固定：

- 可能从 1 位、2 位到 7 位。
- 常规怪物主要集中在 `100000 ~ 9999999`。
- ID 不连续，中间存在大量空洞。
- 可能包含测试怪、任务怪、活动怪、特殊怪。

### 3.3 图片获取方式

图片路径可能类似：

```text
https://osmlib.com/images/monsters/{id}.png
```

实际路径需要以页面 HTML、接口返回或浏览器开发者工具检查结果为准。

## 4. 推荐爬取策略

### 4.1 优先策略：从列表或接口提取 ID

不要盲目遍历大范围 ID。

monster 推荐：

1. 访问怪物列表页。
2. 提取怪物详情页链接。
3. 从链接中解析 monster ID。
4. 进入详情页解析怪物图片 URL。
5. 下载图片并保存 ID、名称、来源、URL。

item 推荐：

1. 访问物品、装备、消耗品、金币或掉落物相关列表页。
2. 提取物品详情页链接或接口数据。
3. 从链接或接口中解析 item ID。
4. 进入详情页解析物品图标、掉落物素材或 PNG URL。
5. 下载图片并保存 ID、名称、类型、来源、URL。
6. 后续统一映射为 YOLO 类别 `item`，不要在 MVP 阶段拆成 coin/equip/consume。

mxdzlk 当前已验证的分页和来源结构如下：

```text
https://mxdzlk.com/monster/?page_num=N
https://mxdzlk.com/item/
https://mxdzlk.com/item/equip/
https://mxdzlk.com/item/?item_type=2
https://mxdzlk.com/item/?item_type=3
https://mxdzlk.com/item/?item_type=4
https://mxdzlk.com/item/?item_type=5
```

当前已落地的爬虫脚本为：

```text
tools/crawl_mxdzlk_assets.py
```

当前脚本会从列表页提取有效 monster/item ID 和静态图片 URL，不做大范围 ID 暴力遍历。

### 4.2 全量爬取执行策略：分阶段、可恢复、避免交互超时

当前 mxdzlk 的 monster/item 列表页数量较多，且建议每次请求间隔 ≥ 1 秒；如果一次性执行：

```bash
python tools/crawl_mxdzlk_assets.py --kind all --max-pages 0 --delay 1
```

会先扫描全部 monster、普通 item、equip、多个 item_type 来源，再下载全部图片。该流程请求量较大，完整耗时可能达到数十分钟到数小时；在 IDE、自动化工具或有 10 分钟命令超时限制的交互环境中，容易出现“发现阶段尚未结束就被杀掉”的情况。因此不建议把全量爬取作为一次短交互命令执行。

推荐按阶段执行：

```bash
# 1. 先完整爬 monster，优先服务打怪识别 MVP
python tools/crawl_mxdzlk_assets.py --kind monster --max-pages 0 --delay 1

# 2. 再分批爬 item，避免一次扫完所有来源
python tools/crawl_mxdzlk_assets.py --kind item --item-sources item --max-pages 0 --delay 1
python tools/crawl_mxdzlk_assets.py --kind item --item-sources equip --max-pages 0 --delay 1
python tools/crawl_mxdzlk_assets.py --kind item --item-sources type2 --max-pages 0 --delay 1
python tools/crawl_mxdzlk_assets.py --kind item --item-sources type3 --max-pages 0 --delay 1
python tools/crawl_mxdzlk_assets.py --kind item --item-sources type4 --max-pages 0 --delay 1
python tools/crawl_mxdzlk_assets.py --kind item --item-sources type5 --max-pages 0 --delay 1
```

推荐运行原则：

- 优先完成 `monster` 全量爬取，因为 MVP 的打怪检测首先依赖怪物外观覆盖。
- `item` 可以按来源分批执行，先爬普通 item 与 equip，再补充其他 item_type。
- 脚本已支持断点式重复执行：本地已存在的图片不会重复下载，`metadata.csv` 会合并更新。
- 如果只想验证流程，使用 `--max-pages 1~5` 或 `--max-assets 20~100`。
- 如果要做完整数据集，建议在普通终端中长期运行，或把标准输出重定向到日志文件。
- 若中途被终止，重新执行同一命令即可继续补齐，已有文件会标记为 `exists`。

### 4.3 备用策略：使用公开 ID 数据集

可以从开源社区或资源项目获取怪物 ID 列表，例如：

- MapleStoryResources 类项目。
- WZ 资源解析结果。
- 社区整理的 monster/item CSV/JSON。
- 游戏资源数据库导出的怪物与物品 ID 清单。

拿到 ID 清单后，再拼接详情页或图片 URL 批量下载。

### 4.4 不推荐策略：暴力猜测遍历

不推荐：

```python
for mob_id in range(100000, 10000000):
    download(mob_id)
```

原因：

- 大量 ID 不存在。
- 请求量过大。
- 容易触发反爬。
- 效率低。
- 对目标站不友好。

## 5. 爬虫注意事项

### 5.1 请求频率

建议：

- 每次请求间隔 ≥ 1 秒。
- 出错后指数退避。
- 不并发或低并发。
- 遇到 403/429 时自动暂停。

### 5.2 请求头

设置合理 User-Agent：

```text
Mozilla/5.0 ...
```

必要时设置：

- Referer
- Accept
- Accept-Language

### 5.3 失败重试

应记录失败 ID 和失败原因：

- 404：资源不存在。
- 403：可能防盗链或反爬。
- 429：请求过快。
- timeout：网络问题。
- 图片格式不符合预期。

### 5.4 文件命名

建议保存结构：

```text
assets/
  monsters/
    raw/
      100100.png
      9300000.png
    metadata.csv

  items/
    raw/
      4000000.png
      2000000.png
    metadata.csv
```

monster 的 `metadata.csv` 字段建议：

```text
id,name,source,page_url,image_url,file_path,status
```

item 的 `metadata.csv` 字段建议：

```text
id,name,item_type,source,page_url,image_url,file_path,status
```

说明：

- `item_type` 可记录原始类型，例如 coin/equip/consume/etc。
- YOLO 训练阶段仍统一映射为类别 `item`。

### 5.5 合规和礼貌爬取

- 遵守目标网站 robots、服务条款和版权要求。
- 仅用于个人研究或内部实验。
- 保留素材来源记录。
- 不要高频请求影响网站服务。

## 6. 快速验证 URL

可手动在浏览器验证少量测试链接。

mxdzlk 当前已验证示例：

```text
https://mxdzlk.com/monster/?page_num=1
https://static.mapleartale.com/images/mob/100100.png
https://mxdzlk.com/item/
https://static.mapleartale.com/images/item/2000000.png
```

osmlib 后续扩展时再以页面 HTML、接口或开发者工具实际结果为准，例如：

```text
https://osmlib.com/?locale=zh-CN
```

注意：

- 单个 URL 可用不代表全站路径永久固定。
- mxdzlk 当前可用图片主域是 `static.mapleartale.com`。
- 部分站点可能开启防盗链或调整页面结构。
- 程序应优先以列表页/接口解析结果为准，而不是依赖旧路径或大范围猜 ID。

## 7. 爬虫数据如何转成训练可用数据

### 7.1 不要把网站素材原样当最终截图训练

错误方式：

```text
透明背景怪物 PNG/物品图标 -> 原样作为训练图片 -> 标注整张素材 -> 直接上线
```

这种方式和真实游戏截图差异太大，不建议直接使用。

正确定位：

```text
怪物 PNG/物品素材 -> 与真实/半真实地图背景合成 -> 自动生成 YOLO 标签 -> 加入训练集
```

也就是说，网站素材可以作为 monster 和 item 两个数据集的主要素材来源，但必须经过合成和域随机化后才能成为有效训练样本。

### 7.2 推荐方式：合成数据

推荐流程：

1. 收集真实地图背景截图，尽量选择无怪物、无掉落物或少目标画面。
2. 从爬虫素材中读取怪物透明 PNG。
3. 从爬虫素材中读取物品图标、掉落物 PNG 或其他 item 素材。
4. 随机缩放到符合游戏内实际尺寸。
5. 将 monster 随机放置到合理地面/平台附近。
6. 将 item 随机放置到地面、平台、怪物附近或角色可拾取区域。
7. 添加轻微亮度、对比度、模糊、透明度扰动。
8. 随机模拟边缘裁切、部分遮挡、伤害数字遮挡、技能特效遮挡。
9. 自动生成 YOLO 标注框。
10. 合成图只放入训练集，不放入验证集和测试集。

### 7.3 合成数据比例

考虑真实截图无法穷尽，本项目建议提高网站素材合成数据占比。

建议：

- 第一版训练中，网站 monster/item 素材合成数据占训练集 60%~80%。
- 真实截图占训练集 20%~40%，主要用于校准域差异、背景误检和 item 小目标。
- 验证集和测试集建议 100% 使用真实截图，至少应以真实截图为主。
- 如果合成数据导致误检增加，应降低比例、增加真实负样本，或改进合成质量。

### 7.4 合成时需要注意的域差异

需要尽量模拟：

- 游戏内实际缩放比例。
- 地图背景复杂度。
- 角色技能特效遮挡。
- 伤害数字遮挡。
- 怪物被边缘裁切。
- 怪物之间重叠。
- 地面和平台高度。
- 真实截图的压缩、锐化或缩放效果。

不建议：

- 随机把怪物贴到天空、UI 区域、聊天框、血条区域。
- 使用过强旋转、透视、上下翻转。
- 合成完全不符合游戏物理位置的样本。

## 8. 掉落物 item 数据说明

`item` 是与 `monster` 并列的核心类别，不能只作为补充数据处理。

item 数据来源建议：

1. **网站物品/掉落物素材**
   - 从 mxdzlk/osmlib 或其他资料库收集金币、装备、消耗品、其他可拾取物素材。
   - 保存原始 item ID、名称、类型、来源 URL。
   - 训练时统一映射为类别 `item`。
   - 如果网站素材与地面掉落形态接近，可以大量用于合成数据。

2. **真实地面掉落物截图**
   - 用于确认网站物品图标和游戏内地面掉落物是否一致。
   - 用于校准小目标尺寸、亮度、透明度、背景干扰。
   - 用于验证 item 在真实画面中的召回率。

3. **合成 item 数据**
   - 将物品素材贴到地图地面、平台或怪物死亡附近。
   - 根据实际游戏表现调整尺寸、阴影、透明度和位置。
   - 自动生成 YOLO 标签。

MVP 阶段建议：

- 所有可拾取掉落物统一标注为 `item`。
- 不区分金币、装备、消耗品。
- 采集或合成多种地图背景上的掉落物。
- 对小目标保持一致标注标准。

如果后续需要更细策略，可扩展类别：

```text
0: monster
1: coin
2: equip
3: consume
4: other_item
```

但第一版不建议拆太细，否则样本需求会显著增加。

## 9. 数据闭环建议

推荐完整流程：

```text
爬取 mxdzlk/osmlib 怪物素材和 ID 清单
  -> 爬取 mxdzlk/osmlib 或其他资料库的物品/掉落物素材和 ID 清单
  -> 采集空地图/少怪地图背景
  -> 采集少量真实战斗截图和真实掉落物截图
  -> 使用网站 monster/item 素材生成大规模合成数据
  -> 人工标注真实截图中的 monster/item
  -> 训练 v0 模型
  -> 在真实录屏/截图上推理
  -> 收集漏检、误检、高不确定样本
  -> 人工修正补标
  -> 训练 v1/v2
  -> 使用真实 test 集验收
```

关键要求：

- 每次训练记录数据版本。
- 每次模型记录训练参数和指标。
- 每轮迭代保留固定 test 集，避免只对当前训练集过拟合。
- 新增地图或怪物后，需要补充真实截图。

## 10. 推荐目录结构

```text
MapleStoryAI/
  assets/
    monsters/
      raw/
      metadata.csv
    items/
      raw/
      metadata.csv

  datasets/
    real/
      images/
      labels/
    synthetic/
      images/
      labels/
    yolo/
      images/
        train/
        val/
        test/
      labels/
        train/
        val/
        test/
      data.yaml

  tools/
    crawl_mxdzlk_assets.py
    make_synthetic_dataset.py
    split_dataset.py
```

说明：

- `assets/monsters/raw/` 保存爬虫怪物素材。
- `assets/items/raw/` 保存爬虫物品/掉落物素材。
- `datasets/real/` 保存真实截图和人工标注。
- `datasets/synthetic/` 保存 monster/item 合成图和自动标签。
- `datasets/yolo/` 保存最终 YOLO 训练结构。
- 验证集和测试集只从 `datasets/real/` 中划分。

## 11. 最终判断

在“真实截图无法穷尽”的前提下，爬虫方案不仅可行，而且应作为 `monster` 和 `item` 两个核心数据集的外观覆盖来源。

可行落地方式：

- 第一阶段优先使用已验证并已落地的 mxdzlk 爬虫获取 monster/item 素材；执行时应按 `monster`、`item` 来源分阶段爬取，而不是依赖一次性短交互全量命令。
- 后续再扩展 osmlib 或公开 ID 数据集，提高怪物和物品外观覆盖率。
- 用爬虫 monster/item 素材生成大规模合成训练数据。
- 用少量真实截图校准域差异、确认 item 地面形态、收集负样本和做最终验收。
- 使用真实截图验证集/测试集作为最终验收依据。

因此，修订后的整体方案是可行的；原先如果计划“把透明 PNG 或物品图标原样直接训练后上线”，则不可行。正确路线是“网站素材主导 monster/item 外观覆盖，合成数据负责场景扩展，真实截图负责校准和验收”。
